{
  "name": "@turborepo-boilerplate/db",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "db:local": "turso dev --db-file local.db",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:local": "TURSO_DATABASE_URL=http://127.0.0.1:8080 drizzle-kit migrate",
    "db:sync": "pnpm db:generate && pnpm db:migrate",
    "db:sync:local": "pnpm db:generate && pnpm db:migrate:local"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.9",
    "tsdown": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "@paralleldrive/cuid2": "^3.3.0",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "drizzle-zod": "^0.8.3",
    "zod": "catalog:"
  }
}
