# Public URL where users will access the app.
# Used at web build time and server runtime.
PUBLIC_APP_URL=https://your-domain.com

# Set true only if you want to expose /docs and /openapi.json publicly.
ALLOW_OPENAPI=false
